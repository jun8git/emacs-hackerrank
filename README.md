Emacs plugin for HackerRank code compile and basic test
-------------------------------------------------------

Add

<code>
(load "path to hackerrank.el")
</code>

to your .emacs file
Restart emacs


shortcut fn + f7
